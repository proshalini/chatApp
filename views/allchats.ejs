<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link rel="stylesheet" href="style.css" />
</head>

<body>
    <h1>All chats !!! :)</h1>
    <a href="http://localhost:8080/chats/new"><button class="create">create chat</button></a>
    <% for (let chat of chats) {%>
        <div class="all">
            <h4>From:<%=chat.from%>
            </h4>
            <h3 class="msg">
                <%=chat.msg%>
            </h3>
            <h4>Received by:<%=chat.to%>
            </h4>
            <hr />
            <p>
                <%=chat.created_at.toString().split(" ")[4]%></p>
            <p><%=chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
            <div class="options">
                    <a href=" http://localhost:8080/chats/<%=chat._id%>/edit"><button class="edit">Edit</button></a>
                    <form method="POST" action="/chats/<%=chat.id%>?_method=DELETE"><button class="edit">delete</button>
                    </form>
        </div>

        </div>
        <br />
        <%}%>
</body>

</html>